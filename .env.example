# =========================================================================
# UniFi Network Health Analyzer - Environmental Configuration Template
# =========================================================================

# --- 1. UniFi Network Controller Connection Settings ---
# The IP address or hostname of your UniFi Controller, UDM, or Cloud Key
UNIFI_HOST=unifi-controller.local

# The API port of the UniFi Controller (usually 8443, or 443 on modern UDM/UXG)
UNIFI_PORT=8443

# Controller credentials (A Read-Only/Observer user is highly recommended!)
UNIFI_USER=your_observer_username
UNIFI_PASS=change_me

# UniFi site ID (default is 'default'; look at your browser URL in UniFi to verify)
UNIFI_SITE=default

# --- 2. Application Dashboard Settings ---
# Internal server port (keep as 3445 inside Docker containers)
PORT=3445

# Controller cache duration in seconds. Minimizes hits/load on your controller API
CACHE_EXPIRY_SEC=15

# Optional: enforce API access to /api/* with a token in x-api-token header
API_TOKEN=

# Optional: only set true when you knowingly trust a self-signed controller cert
UNIFI_ALLOW_SELF_SIGNED=false

# --- 3. Dashboard Admin Credentials ---
# ⚠️ CHANGE THESE DEFAULTS before exposing the dashboard
ADMIN_USER=admin
ADMIN_PASS=admin

# --- 4. Optimization Settings ---
# Maximum APs the batch optimizer suggests per round (default 8)
OPT_MAX_CHANGES=8

# --- 5. Security & Environment ---
# Set to "production" to enable Secure flag on session cookies (requires HTTPS)
NODE_ENV=

# --- 6. Docker Orchestration Host Port ---
# The unique port on the host (e.g. your Proxmox Debian LXC) where the app is exposed
HOST_PORT=2943

# --- 7. Development / Testing ---
# Enable mock mode to simulate 12 APs + 60 clients without a real controller
# MOCK_MODE=true
# Enable stacked mock mode to simulate the real school channel-stacking problem
# (80% of 5 GHz APs on ch 40/44 — useful for testing the optimizer)
# MOCK_STACK_MODE=true
